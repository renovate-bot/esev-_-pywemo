{
    "postUpgradeTasks": {
        "commands": [
            "bash -c '. scripts/common.sh && poetryInstall'",
            "poetry run pre-commit run"
        ],
        "fileFilters": [
            "pyproject.toml",
            "poetry.lock",
            "**/*.py",
            "scripts/bootstrap-requirements.txt"
        ]
    }
}
